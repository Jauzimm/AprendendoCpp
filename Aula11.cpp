#include <iostream>
#include <cmath>


int main () {

    // double x = 3, y = 4, z = 0;
    // This functions can be used without the library <cmath>
    //z = std::max(x, y);
    //z = std::min(x, y);

    //Using cmath
    // z = pow(2, 4);
    // z = sqrt(9);
    // z = abs(-3);
    // z = round(x);
    // z = ceil(x);
    // z = floor(x);

    return 0;
}